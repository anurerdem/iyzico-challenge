package com.iyzico.challenge.service;

//@RunWith(SpringRunner.class)
//@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
public class SeatControllerIntegrationTest {
/*
    @Autowired
    private TestRestTemplate restTemplate;

    @Sql({ "create-db.sql", "insert-data.sql" })
    @Test
    public void addSeat()
    {
        SeatCreateRequest seatRequest= new SeatCreateRequest();
        seatRequest.setId(1L);
        HttpEntity<?> httpEntity = new HttpEntity<Object>(seatRequest);
        ResponseEntity<List<SeatResponseResponse>> responseEntity = restTemplate.exchange(
                "/api/v1/seat", HttpMethod.POST, httpEntity,
                new ParameterizedTypeReference<List<SeatResponseResponse>>(){});
        List<SeatResponseResponse> seat = responseEntity.getBody();
        SeatResponseResponse deneme = new SeatResponseResponse();
        deneme.setId(1L);

    }

   */

}
